services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/data/crypto_app.db
    volumes:
      - ./data:/app/data
      - ./uploads:/app/app/static/uploads
